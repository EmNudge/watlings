{
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bash run.sh",
    "build": "node build.mjs",
    "test": "vitest --run"
  },
  "devDependencies": {
    "vitest": "^0.34.1"
  },
  "dependencies": {
    "wabt": "^1.0.32"
  }
}
